# Copy this file to secrets.toml for LOCAL runs, OR paste its contents into
# Streamlit Community Cloud -> Advanced settings -> Secrets.
# NEVER commit the real secrets.toml (it is git-ignored).

[connections.snowflake]
account = "EBLBATM-OUB79121"
user = "devday1"
password = "<your-password>"     # or use key-pair / authenticator
warehouse = "COMPUTE_WH"
database = "PMC_ANALYSIS"
schema = "PUBLIC"
